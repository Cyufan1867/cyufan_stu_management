<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cyufan.mapper.ClazzMapper">
    <select id="list" resultType="com.cyufan.pojo.Clazz">
        select
        c.*,
        e.name masterName,
        (case when begin_date &gt; now() then '未开班' when now() &gt; end_date then '已结班' else '在读中' end) status
        from clazz as c left join emp e on c.master_id = e.id
        <where>
            <if test="name != null and name != ''">
                c.name like concat('%',#{name},'%')
            </if>
            <if test="begin != null and end !=null">
                and c.begin_date between #{begin} and #{end}
            </if>
        </where>
        order by c.update_time desc
    </select>
</mapper>
